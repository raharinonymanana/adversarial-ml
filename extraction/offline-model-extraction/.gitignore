# Trained model artifacts — regenerated by the scripts, never committed.
models/

# Pickled artifacts (clone, victim, test set, attacker pool, results).
*.pkl

# Python bytecode caches.
__pycache__/
*.pyc
