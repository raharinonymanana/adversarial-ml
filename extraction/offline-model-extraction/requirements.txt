# Lab 3 — Offline Model Extraction
# Everything below runs on CPU only — no GPU required.
# No pandas: the dataset is sklearn's built-in digits (in-memory arrays, no CSV).

scikit-learn>=1.3.0      # MLP victim, Random Forest clone, digits dataset, metrics
numpy>=1.24.0            # array math
matplotlib>=3.7.0        # notebook charts (budget curve, digit images)
seaborn>=0.12.0          # nicer notebook visualizations
jupyter>=1.0.0           # run the exploration notebook
